$('.owl-carousel').owlCarousel({
    loop:true,
    margin:10,
    items: 5,
    nav: true,
    navText: ["<<", ">>"],
    responsiveClass:true
})

$('.owl-carousel').owlCarousel({
	baseClass: "owl-quot",
    loop:true,
    items: 1,
    nav: true,
    navText: ["<<", ">>"],
    responsiveClass:true
})